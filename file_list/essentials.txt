List of modules:

Flash			->				Used by blackbox and cli. 
								cli not required because we have Linux on edison. 
								It's is used for changing parameters during runtime. Not essential.
								Black box not part of essential features
EEPROM			->				

GPIO			->				Use IOInitGlobal() from /src/main/drivers/io.c for initializing GPIO pins 
								through MRAA using data stored in GPIO_TypeDef struct
